apiVersion: v1
kind: Service
metadata:
  name: {{ include "connectormetrics.fullname" . }}
  labels:
    {{- include "connectormetrics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "connectormetrics.selectorLabels" . | nindent 4 }}
