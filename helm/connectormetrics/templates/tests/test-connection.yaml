apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "connectormetrics.fullname" . }}-test-connection"
  labels:
    {{- include "connectormetrics.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "connectormetrics.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
